---
###########################################################################
# 📡 OpenEMS FDTD-Simulation Installation und Konfiguration
# Multi-OS | idempotent | AWX-kompatibel
###########################################################################
# Credentials (AWX):
#   allg_status:      aktiviert | deaktiviert | deinstalliert
#   install_mode:     docker | nativ (Docker-Container oder native Installation)
#   ssh_key:          SSH-Zugangsdaten
#
# Beschreibung:
# Installiert OpenEMS (Open Electromagnetic Field Solver)
# für FDTD-Simulationen (Finite Difference Time Domain).
# Unterstützt native Installation (aus Source) und Docker-Variante.
#
# Hinweise:
# - Native: Build aus Source (cmake, boost, hdf5, vtk, etc.)
# - Docker: Custom OpenEMS Docker Image
# - Benötigt ausreichend RAM (min. 8 GB empfohlen)
# - CSXCAD (Geometry Library) wird als Dependency installiert
#
# BEST PRACTICES:
# - Wenn möglich kein Code im Playbook, sondern in pre_task/task auslagern
# - Nicht benötigte Service-Includes löschen (nicht auskommentieren)
###########################################################################

- name: pb_setup_openems.yml
  hosts: all
  become: true
  gather_facts: true

  ###########################################################################
  # Externe Variablen (OS-spezifische Daten)
  ###########################################################################
  vars_files:
    - playbooks_paths.yml

  vars:
    # Hauptanwendung(en)
    var_pack_name:
      - openems

    # Abhängigkeiten
    var_dependencies_list:
      - openems_deps

    # Voraussetzungen
    var_prerequisites:

    # Installationsreihenfolge: 1 = erst Dependencies dann Hauptanwendung
    var_inst_order: 1

    # Kein Service (OpenEMS ist kein Daemon)
    var_sonder_start:
    var_sonder_autostart:

    # Nicht unterstützte OS
    unsupported_systems:
      - Suse
      - Archlinux
      - FreeBSD

    # Playbook-Kontext
    templates_paths_pb: openems

    # Playbook-Log-Dokumentation
    pb_doku_template: openems_doku

  #############################################################################

  pre_tasks:
    ###########################################################################
    # Variablen laden
    ###########################################################################
    - name: Lade alle Variablen-Dateien - pb_setup_openems.yml
      ansible.builtin.include_vars:
        file: "{{ item }}"
      loop: "{{ var_files }}"

    ###########################################################################
    # Hook: Pre-Start
    ###########################################################################
    - name: 🔧 Hook Pre-Start
      ansible.builtin.include_tasks: "{{ include_paths.hook_pre_start }}"

    ###########################################################################
    # Systemcheck & Prerequisites
    ###########################################################################
    - name: 🔄 Importiere Systemcheck
      ansible.builtin.include_tasks: "{{ include_paths.systemcheck }}"

    - name: 🔄 Importiere Systemcheck zwingend benötigter Komponenten
      ansible.builtin.include_tasks: "{{ include_paths.prereq_check }}"

    ###########################################################################
    # Standard Pre-Tasks
    ###########################################################################
    - block:
        - name: 🔄 Importiere Paketmanager-Lock prüfen
          ansible.builtin.include_tasks: "{{ include_paths.check_pkg_lock }}"

        - name: 🔄 Importiere Debug ausgabe bei Playbook start
          ansible.builtin.include_tasks: "{{ include_paths.debug_start }}"

    ###########################################################################
    # Playbook-spezifische Pre-Tasks
    ###########################################################################
    - block:
        - name: 🔄 Importiere pre_task
          ansible.builtin.include_tasks: "{{ task_paths.openems_pretask }}"
      when: allg_status == "aktiviert"

  tasks:
    ###########################################################################
    # HAUPTLOGIK - Installation
    ###########################################################################
    - block:
        - name: 🔧 Hook Pre-Install
          ansible.builtin.include_tasks: "{{ include_paths.hook_pre_install }}"

        - name: 🔄 Komplette Installation
          ansible.builtin.include_tasks: "{{ include_paths.install_allg }}"

        - name: 🔄 Playbook-spezifische Konfiguration
          ansible.builtin.include_tasks: "{{ task_paths.openems_task }}"

      when: allg_status == "aktiviert"

    ###########################################################################
    # Deaktivierung
    ###########################################################################
    - block:
        - name: 🔄 Dienste stoppen
          ansible.builtin.include_tasks: "{{ include_paths.services_stop }}"
      when: allg_status == "deaktiviert"

    ###########################################################################
    # Deinstallation
    ###########################################################################
    - block:
        - name: 🔄 Deinstallation
          ansible.builtin.include_tasks: "{{ include_paths.deinstall_allg }}"

        - name: 🔄 Cleanup
          ansible.builtin.include_tasks: "{{ include_paths.cleanup }}"
      when: allg_status == "deinstalliert"

    ###########################################################################
    # Hook: end
    ###########################################################################
    - name: 🔧 Hook Post-Install
      ansible.builtin.include_tasks: "{{ include_paths.hook_end_pb }}"
